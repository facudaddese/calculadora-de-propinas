export interface MenuItem {
  id: number;
  title: string;
  price: number;
  description: string;
  category: string;
  image: string;
  rating: {
    rate: number;
    count: number;
  };
}

export interface OrderItem extends MenuItem {
  quantity: number;
}
